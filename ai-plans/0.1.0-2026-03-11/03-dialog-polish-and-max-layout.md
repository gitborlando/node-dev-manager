# 03 Dialog Polish And Max Layout

## 目标

- 去掉标题栏里的 `Node Dev Manager` 文案。
- 修复最大化后整个内容区和屏幕之间出现空隙的问题。
- 继续打磨项目模态框，让圆角和按钮体系更一致。

## 执行

1. 标题栏品牌区只保留图标，移除文字占位。
2. 最大化安全区只保留在标题栏右侧，不再给整个壳层内容加外边距。
3. 模态框外层圆角和内部控件圆角整体收窄，向主界面按钮的 `6px` 体系靠拢。
4. 调整模态框遮罩、投影、输入底色和交互层次，让视觉更轻。

## 校验

- `pnpm typecheck`
- `pnpm build`
