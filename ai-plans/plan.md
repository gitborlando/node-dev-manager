你现在是一个资深桌面端全栈工程师、系统设计师和代码重构专家，擅长 Tauri、React、TypeScript、Node.js、monorepo 架构设计，以及本地进程管理工具开发。

我要你基于现有项目，重构并实现一个 Tauri 本地软件：一个用于统一管理本地开发进程的进程管理器。这个工具的目标是替代我同时打开很多 cmd 窗口的工作方式，提供更清晰、更稳定、更易扩展的本地进程管理体验。

在开始任何设计和实现之前，你必须严格遵守以下执行顺序：

1. 先完整阅读并遵守 AGENTS.md 中的所有规范
2. 再分析两个以 node_dev 开头的文件，理解之前的前端和后端逻辑和视觉风格(紧凑简洁)
3. 然后分析我提供的 React 组件，尽量复用其设计语言、交互方式
4. 最后参考 vite.config.ts，除非有明确必要，否则配置尽量沿用

## 项目目标

请基于现有项目，设计并实现一个 monorepo 架构的 Tauri 本地应用，用于管理多个本地开发进程。核心目标包括：

- 统一启动、停止、重启多个本地服务或命令
- 替代分散的 cmd 窗口
- 清晰展示每个进程的状态、日志、端口、启动命令、运行目录等信息
- 支持稳定、清晰、可维护的前后端通信机制
- 在现有 Node_dev 逻辑基础上升级，而不是无视已有实现直接推倒重来

## 已知上下文

- AGENTS.md 是最高优先级规范，必须先读
- 两个 Node_dev 开头的文件分别代表之前的前端与后端逻辑
- 当前需要解决的重点问题之一是：前后端需要用比之前更好的通信方式
- 我已经提供了一些 React 组件，你应按需复用，并尽量保持其风格一致
- vite.config.ts 基本全部沿用即可, auto-import去掉不需要的
- 整个项目必须采用 monorepo 架构

## 技术要求

- 前端使用 React + TypeScript
- 桌面端使用 Tauri
- 项目结构使用 monorepo
- 优先保证类型安全、可维护性、可扩展性
- 不要引入不必要的复杂依赖
- 如果旧逻辑不合理，可以重构，但必须说明为什么重构、替代方案是什么、迁移成本如何
- 通信设计必须优先考虑：
  - 职责边界清晰
  - 状态同步可靠
  - 日志流和进程事件处理稳定
  - 便于后续扩展为更多进程控制能力
- 不要只给概念，必须结合项目结构给出可落地方案

## 你必须完成的工作

请按以下阶段输出，不要跳步：

### 第一阶段：理解与审查

- 总结 AGENTS.md 中与你这次任务最相关的约束
- 分析两个 Node_dev 文件分别做了什么
- 判断现有前后端通信方式的问题在哪里
- 分析现有 React 组件的风格特征，以及适合复用到哪些页面/模块
- 总结 vite.config.ts 中必须保留的配置点

### 第二阶段：架构设计

给出一个适合本项目的 monorepo 目录结构，并解释每个 package / app 的职责。至少要覆盖：

- Tauri 桌面应用壳
- React 前端应用
- 进程管理核心逻辑
- 共享类型或共享工具模块
- 可选的状态管理或事件通信层

同时明确说明：

- 前后端最终如何通信
- 为什么这种通信方式优于原来的 Node_dev 方案
- 进程启动、停止、日志推送、状态变更分别如何流转
- 如何处理多个并发进程
- 如何避免 UI 和进程控制逻辑耦合过深

### 第三阶段：实现方案

在现有项目基础上给出具体改造计划，包括：

- 哪些旧文件保留
- 哪些旧文件重构
- 哪些模块拆分
- 哪些能力抽象成共享层
- 每一步改造的顺序

### 第四阶段：代码实现

基于上面的方案直接编写或改写代码。要求：

- 优先输出真实可运行代码
- 所有代码使用 TypeScript
- 保持与现有风格一致
- 尽量复用已有组件
- 不要省略关键实现
- 如果篇幅过长，可以按模块分批输出，但每一批都必须完整可落地

### 第五阶段：说明与验证

最后补充：

- 如何运行项目
- 如何验证进程管理器功能是否正常
- 关键设计决策说明
- 后续可以继续扩展的方向

## 输出风格要求

- 先分析再实现，不要一上来就直接给代码
- 尽量基于现有文件做增量改造
- 明确指出你的假设，避免假装看过实际上没看到的文件内容
- 如果某些文件内容不足以支撑实现，要明确说明缺失点
- 输出要结构化，使用清晰的小节标题
- 不要空泛，不要只讲最佳实践，必须贴合当前项目
- 不要偷换技术栈
- 不要忽略 AGENTS.md 的规范
